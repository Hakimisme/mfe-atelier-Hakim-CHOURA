import React from "react";

const Header = () => {
  return (
    <header style={{ background: "blue", padding: "10px", color: "white" }}>
      Micro Frontend Header
    </header>
  );
};

export default Header;
